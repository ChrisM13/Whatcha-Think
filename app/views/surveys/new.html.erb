<h1>Create Survey</h1>
<%= form_for @survey, url: surveys_path do |f| %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions"><%= f.submit "Submit" %></div>
<% end %>